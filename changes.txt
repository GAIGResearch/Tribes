Board.java

- TreeMap<int, actor> gameActors;
     * Holds all the actors in the game.


GameState.java and Board.java

 - New functions:

    public int addActor(Actor actor) {...}

        To add a new actor to the list of game actors. It returns the unique identifier of this actor for the rest of the game:

    public Actor getActor(int actorId) { ... }

        Returns the actor with the associated tribeId. Null if not in game.

    public boolean removeActor(int actorId) {...}

        Removes the actor with the associated tribeId. True if it could be removed (false may mean it wasn't in game).






